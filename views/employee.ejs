<!DOCTYPE html>
<html lang="en">
 
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Information</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@1.0.2/css/bulma.min.css">
    <link rel="icon" type="image/png" href="/images/favicon.png">
    <style>
        .navbar-item img {
            height: 50px;
            width: 50px;
            border-radius: 50%;
            object-fit: cover;
        }
 
        .profile-picture {
            margin-left: auto;
            display: flex;
            align-items: center;
        }
    </style>
</head>
 
<body>
    <!-- Header -->
    <nav class="navbar has-background-white" role="navigation" aria-label="main navigation" style="padding: 1rem;">
        <div class="navbar-brand">
            <!-- Logo in its own div -->
            <div style="display: flex; align-items: center; padding: 0.5rem;">
                <a href="/">
                    <img src="/images/kainoslogo.jpg" alt="Kainos logo" style="height: 100px; width: auto;">
                </a>
            </div>
        </div>
 
        <div id="navbarBasicExample" class="navbar-menu">
            <div class="navbar-start">
                <a class="navbar-item" href="\">Back</a>
            </div>
            <div class="navbar-end">
                <div class="navbar-item">
                    <div class="buttons">
                        <a href="/logout" class="button is-primary is-outlined">Log Out</a>
                    </div>
                </div>
            </div>
        </div>
    </nav>
 
    <!-- Main Section -->
    <section class="section has-background-light">
        <div class="container">
            <h1 class="title has-text-centered">Employee Information</h1>
 
            <% if (employee) { %>
                <div style="display: flex; justify-content: space-between; align-items: flex-start;">
                <table class="table is-striped is-fullwidth">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Address</th>
                            <th>Role</th>
                            <th>Salary</th>
                            <th>Employee Number</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td><%= employee.name %></td>
                            <td><%= employee.address %></td>
                            <td><%= employee.role %></td>
                            <td><%= employee.salary %></td>
                            <td><%= employee.empNum %></td>
                        </tr>
                    </tbody>
                </table>
                <div style="margin-left: auto; margin-right: auto; transform: translateX(50%);">
                    <img src="/images/profile.png" alt="Employee Profile Picture" style="height: 150px; width: 150px; border-radius: 50%; object-fit: cover;">
                </div>
               
            </div>
            <% } else { %>
                <p class="has-text-centered">No user found.</p>
            <% } %>
        </div>
    </section>
 
    <!-- Footer -->
    <footer id="footer" class="footer" style="min-height: 100vh; background-image: url('/images/footerphoto.jpg'); background-size: cover; background-position: left; display: flex; align-items: top; justify-content: center; color: white; margin: 0;">
        <div class="content has-text-centered">
            <p>&copy; 2025 Kainos. All rights reserved.</p>
        </div>
    </footer>
</body>
 
</html>
